name: Feature
body:
  - type: dropdown
    id: domain
    attributes:
      label: Domain
      options: [oracle, muse, mediator, memory, audit, router]
  - type: textarea
    id: spec
    attributes:
      label: What & Why