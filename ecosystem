Python Code import zipfile, os
# Path for updated full ecosystem with embodiment folder included
ecosystem_full_with_embodiment = "/mnt/data/ecosystem_full_with_embodiment.zip"
with zipfile.ZipFile(ecosystem_full_with_embodiment, "w", zipfile.ZIP_DEFLATED) as Z:
ror root, - files in os. walk("/mnt/data"):
if anylp in root for p in l
"ecosystem", "ecosystem_with_founders", "ecosystem_rituals",
"ecosystem_codex", "logs", "index", "embodiment"
1) :for
file in files:
full = os. path. join (root, file)
arcname = os. .path.relpath(full, "/mnt/data")
z.write(full, arcname)
ecosystem_full_with_embodiment